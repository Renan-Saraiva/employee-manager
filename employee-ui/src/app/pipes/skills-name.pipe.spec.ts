import { SkillsNamePipe } from './skills-name.pipe';

describe('SkillsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SkillsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
